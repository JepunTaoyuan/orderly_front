I apologize for the misunderstanding. I will restructure the User Info Card to ensure the correct layout as requested.

### Plan
I will modify the `LeaderboardProfile` component to wrap the Name and ID sections in separate containers to achieve the desired vertical stacking of labels and values.

### Changes
1.  **Restructure User Info Section**:
    *   Create a main vertical `Flex` container (column) to hold the two data blocks.
    *   **Block 1 (Name)**: Create a container holding the "Name" label and the Name value ("Chiu Lin").
    *   **Block 2 (ID)**: Create a container holding the "ID" label and the ID value.
    *   Ensure proper spacing (margin/gap) between the Name block and the ID block.

### File to Modify
*   `app/packages/trading-leaderboard/components/leaderboard/profile/leaderboardProfile.ui.tsx`
