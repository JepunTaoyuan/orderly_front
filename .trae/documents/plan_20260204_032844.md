I have analyzed the codebase and identified the necessary steps to implement the "Profile Component" on the leaderboard page.

### Plan Overview
I will create a new `LeaderboardProfile` component and integrate it into the existing `GeneralLeaderboard` widget. The implementation will include mock data fetching as requested.

### Implementation Steps

#### 1. Create Profile Component
**File:** `app/packages/trading-leaderboard/components/leaderboard/profile/leaderboardProfile.ui.tsx`
*   Create a styled component matching the design:
    *   **Header**: "Points You Earned" title with a "Share" button.
    *   **User Info**: Avatar (placeholder), Name (Mock "Chiu Lin"), and ID (Shortened Address).
    *   **Stats Sections**:
        *   "Overall": Total Points & Rank.
        *   "Last Week's Performance": Points & Rank.
*   Use Orderly UI components (`Box`, `Flex`, `Text`, `Button`) to ensure consistent styling.

#### 2. Create Data Hook (with Mock Data)
**File:** `app/packages/trading-leaderboard/components/leaderboard/profile/useLeaderboardProfile.ts`
*   Implement a hook `useLeaderboardProfile`.
*   Retrieve the current user's address using the `useAccount` hook from `@orderly.network/hooks`.
*   Return mock data if API data is not available:
    ```json
    {
      "name": "Chiu Lin",
      "overall": { "points": 120, "rank": 16 },
      "lastWeek": { "points": 102, "rank": 20 }
    }
    ```

#### 3. Integrate into Leaderboard Layout
**File:** `app/packages/trading-leaderboard/components/leaderboard/generalLeaderboard/generalLeaderboard.ui.tsx`
*   Modify the `GeneralLeaderboard` component layout.
*   Switch the main container to a `Flex` layout (on desktop).
*   **Left Side**: Insert the new `<LeaderboardProfile />`.
*   **Right Side**: Wrap the existing Tabs, Filters, and Table in a container taking up the remaining space.
*   **Responsiveness**: Ensure the profile component is hidden or stacked correctly on mobile devices (using `isMobile` check).

#### 4. Verification
*   Verify the layout matches the provided screenshot.
*   Ensure the component displays the mock data correctly.
*   Check that the existing table functionality (sorting, filtering) remains unaffected.
