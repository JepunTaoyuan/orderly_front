I will fix the text alignment issue by using the correct prop for the `Flex` component.

### Changes
1.  **Correct Prop Usage**:
    *   Change `align="start"` to `itemAlign="start"` in the `Flex` component within `LeaderboardProfile`. This will ensure the "Name" and "ID" text blocks are correctly left-aligned.

### File to Modify
*   `app/packages/trading-leaderboard/components/leaderboard/profile/leaderboardProfile.ui.tsx`
