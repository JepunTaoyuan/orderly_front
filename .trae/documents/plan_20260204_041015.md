I will completely overhaul the layout of the "Overall" and "Last Week's Performance" cards to strictly match the provided HTML/CSS structure.

### Plan
I will rewrite the `Box` components for these two cards in `LeaderboardProfile.ui.tsx` to replicate the nested `div` structure and styles from the user's snippet using Orderly UI components (`Box`, `Flex`, `Text`).

### Changes
1.  **Main Container**:
    *   `width: 100%`, `padding: 20px`, `background: rgba(255, 255, 255, 0.05)`, `borderRadius: 4px` (or `rounded-md`).
    *   Flex column, `justify-start`, `align-start`, `gap: 10px`.

2.  **Title Section**:
    *   "Overall" / "Last Week's Performance"
    *   `color: rgba(255, 255, 255, 0.90)`, `fontSize: 13px`, `fontWeight: 700`, `lineHeight: 24px`.

3.  **Stats Row**:
    *   Flex row, `justify-start`, `align-center`, `gap: 16px`.

4.  **Stat Block (Points/Rank)**:
    *   Fixed `width: 125px`, `height: 68px`.
    *   Flex column, `justify-center`, `align-start`, `gap: 8px`.
    *   **Label**: `color: rgba(255, 255, 255, 0.50)`, `fontSize: 13px`, `fontWeight: 700`.
    *   **Value**: `color: #C9BDFF`, `fontSize: 28px`, `fontWeight: 600`.

### File to Modify
*   `app/packages/trading-leaderboard/components/leaderboard/profile/leaderboardProfile.ui.tsx`
